_schema-version: 3.3.0
ID: hrcap
version: 1.0.0
description: "HR CAP Platform Application"
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build shared-db --for hana --production
        # - npx cds build confirm-timesheet --for nodejs --production --ws-pack
        - npm run build:confirm-timesheet
modules:
  - name: confirm-timesheet-srv
    type: nodejs
    path: confirm-timesheet/gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
      memory: 256M
      disk-quota: 512M
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: confirm-timesheet-auth
      - name: confirm-timesheet-db
  - name: confirm-timesheet-db-deployer
    type: hdb
    path: shared-db/gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: confirm-timesheet-db
  - name: confirm-timesheet-app-deployer
    type: com.sap.application.content
    path: confirm-timesheet/gen
    requires:
      - name: confirm-timesheet-auth
      - name: srv-api
      - name: confirm-timesheet-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: confirmtimesheetnoconfirm
          artifacts:
            - no-confirm.zip
          target-path: app/
  - name: confirm-timesheet
    type: approuter.nodejs
    path: confirm-timesheet/app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: confirm-timesheet-auth
      - name: confirm-timesheet-html5-runtime
      - name: confirm-timesheet-destination-service
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}
  - name: confirmtimesheetnoconfirm
    type: html5
    path: confirm-timesheet/app/no-confirm
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []
resources:
  - name: confirm-timesheet-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./confirm-timesheet/xs-security.json
      config:
        xsappname: confirm-timesheet-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
    requires:
      - name: app-api
  - name: confirm-timesheet-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: confirm-timesheet-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: confirm-timesheet-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
  - name: confirm-timesheet-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: ui5
                URL: https://ui5.sap.com
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
      service: destination
      service-name: confirm-timesheet-destination-service
      service-plan: lite
